# 基于 Transformer 的反谐振空芯光纤超快脉冲传播代理建模

## 1. 论文定位

投稿赛道：**AI + Photonics**

目标篇幅：约 **两页 A4 PDF**。

推荐题目：

> 基于 Transformer 的反谐振空芯光纤超快脉冲传播代理建模

英文题目可对应为：

> Transformer-Based Surrogate Modeling of Ultrafast Pulse Propagation in Anti-Resonant Hollow-Core Fibers

备选题目：

1. 基于 UPPE 仿真的反谐振空芯光纤紫外色散波动力学学习
2. 面向反谐振空芯光纤紫外产生的 AI 辅助预测方法
3. 气体填充反谐振空芯光纤中光谱演化的 Transformer 预测模型

建议主线：

> 利用 UPPE 高保真仿真构建反谐振空芯光纤传播数据集，并训练 Transformer 代理模型，从光纤结构、气体条件和输入脉冲参数快速预测光谱演化及紫外色散波特征。

## 2. 核心贡献

建议突出三个贡献点：

1. **物理驱动的数据集构建**
   - 数据来自包含宽带传播和电离效应的 UPPE 仿真，而不是简化解析公式。
   - 数据集覆盖反谐振空芯光纤结构参数、气压、脉冲能量、脉宽等输入条件。

2. **面向传播演化的序列建模**
   - Transformer 模型不仅预测最终输出光谱，还预测完整的传播演化图 `S(z, lambda)`。
   - 通过 z 位置嵌入和波长条件解码器，模型能够学习传播距离与光谱结构之间的关联。

3. **面向紫外波段的专项优化**
   - 对 `200-700 nm` 紫外波段进行单独评估。
   - 可引入 UV 加权损失和早期传播加权损失，以提升紫外色散波相关特征的预测能力。

## 3. 推荐两页论文结构

### 标题、作者与摘要

可以沿用 `摘要.docx` 中的摘要，但建议压缩到约 120-160 个英文单词，避免占用过多版面。

需要注意：

- 只有在最终 Transformer 结果确实支持时，才保留 `R² exceeding 0.9`。
- 如果最佳结果来自 Temporal CNN 或其他模型，应使用更宽泛的表述，例如 **neural surrogate model**，而不是明确写 **Transformer-based surrogate model**。

### 1. 引言

本节目标：说明问题背景和研究动机。

建议包含：

- 反谐振空芯光纤是气体非线性频率转换和相干紫外产生的重要平台。
- 紫外色散波产生受波导色散、Kerr 非线性、等离子体响应和气体电离等多因素共同影响。
- UPPE 可以更完整地描述宽带传播和电离效应，但数值求解计算量大。
- 数据驱动代理模型可以显著加快参数扫描、结构设计和优化过程。

建议贡献句：

> 本文提出一种基于 UPPE 仿真数据训练的 Transformer 代理模型，用于预测气体填充反谐振空芯光纤中的宽带光谱演化和紫外色散波特征。

### 2. 数据集与物理模型

本节目标：说明数据来源及其物理可信度。

建议内容：

- 仿真框架：Luna / UPPE。
- 光纤类型：反谐振空芯光纤。
- 输入参数：
  - 脉冲能量
  - 脉冲宽度
  - 气体压强
  - 纤芯直径
  - 管壁厚度
  - 可选啁啾
- 预测目标：
  - 最终输出光谱
  - 完整光谱传播演化 `S(z, lambda)`
  - 紫外波段 `200-700 nm` 特征
- 训练波长范围：
  - `200-2500 nm`
- 预处理方式：
  - 插值到固定波长网格
  - 归一化 log 光谱或全局 log-power 标准化
  - 训练集、验证集、测试集划分

如果使用 `per_sample_minmax` 数据，应写：

> 模型预测的是逐样本归一化后的 log 光谱形状。

如果使用 `global_log_standard` 数据，应写：

> 模型预测的是全局标准化后的 log-power 光谱，因此保留了样本之间的相对强度差异。

### 3. Transformer 代理模型

本节目标：简洁说明 AI 方法。

建议内容：

- 输入为光纤结构、气体条件和输入脉冲参数组成的特征向量。
- 参数编码器将物理输入映射到潜在空间。
- z 位置嵌入表示传播距离。
- 波长条件解码器输出每个传播位置处的光谱。
- 可选的分波段 decoder 用于提高不同光谱区域的建模能力。

模型输入输出可概括为：

```text
物理输入参数 -> Transformer 代理模型 -> 预测 S(z, lambda)
```

简化损失函数可写为：

```text
L = L_temporal + alpha L_final + beta L_UV + gamma L_lambda-grad + eta L_z-grad
```

其中：

- `L_temporal`：完整传播图 MSE
- `L_final`：最终光谱损失
- `L_UV`：`200-700 nm` 紫外波段损失
- `L_lambda-grad`：波长方向梯度匹配损失
- `L_z-grad`：传播方向梯度匹配损失

两页论文中不建议写过多网络细节，应以模型思想和结果展示为主。

### 4. 结果与讨论

本节目标：证明代理模型有效。

建议展示三类结果：

1. **最终光谱预测**
   - 对比 UPPE ground truth 和 Transformer prediction。
   - 横轴：wavelength，`200-2500 nm`
   - 纵轴根据训练目标选择：
     - normalized log power
     - log power
     - relative dB

2. **光谱传播演化预测**
   - UPPE 仿真传播图
   - 模型预测传播图
   - 误差图
   - 可选展示 `0-10 cm` 早期传播局部放大图

3. **定量指标对比**
   - 可放一个简洁表格：

| 模型 | Final R² | Temporal R² | UV R² | 说明 |
|---|---:|---:|---:|---|
| MLP | TBD | - | TBD | 最终光谱 baseline |
| LSTM | TBD | TBD | TBD | 序列模型 baseline |
| Temporal CNN | TBD | TBD | TBD | 二维传播图 decoder |
| Transformer | TBD | TBD | TBD | 序列感知代理模型 |

建议讨论点：

- 模型能够捕捉主要的光谱展宽和紫外色散波特征。
- 紫外波段预测更困难，因为其局部结构尖锐且对非线性过程敏感。
- 相比只预测最终光谱的 MLP，序列感知模型更适合预测完整传播动力学。
- 如果 random split 性能高而 holdout 性能下降，应说明模型目前主要适合作为采样参数空间内部的插值代理模型。

### 5. 结论

建议结论：

> 本文展示了一种面向气体填充反谐振空芯光纤超快脉冲传播的 Transformer 代理建模方法。该模型基于 UPPE 高保真仿真数据训练，能够预测宽带光谱传播演化及紫外色散波相关特征，并显著降低推理成本。该方法可用于快速参数扫描、反谐振空芯光纤结构设计以及光纤超快紫外光源优化。

## 4. 推荐图表

### 图 1：整体工作流程

建议结构：

```text
输入物理参数
      |
      v
AR-HCF 中的 UPPE 仿真
      |
      v
HDF5 数据集与预处理
      |
      v
Transformer 代理模型
      |
      v
最终光谱与 S(z, lambda) 预测
```

图注：

> 图 1. 基于 UPPE 仿真的反谐振空芯光纤光谱演化 AI 代理建模流程。

### 图 2：预测结果

建议子图：

- 图 2(a)：最终光谱，UPPE 与 Transformer 对比
- 图 2(b)：UPPE 光谱传播演化
- 图 2(c)：Transformer 预测传播演化
- 图 2(d)：绝对误差图

图注：

> 图 2. UPPE 仿真结果与 Transformer 代理模型预测结果对比。模型在 `200-2500 nm` 波长范围内能够复现主要的光谱展宽行为和紫外色散波特征。

## 5. 两页排版建议

第一页：

- 标题、作者、摘要：约 25%
- 引言：约 25%
- 数据集与物理模型：约 25%
- 图 1：约 25%

第二页：

- Transformer 模型：约 25%
- 结果与图 2：约 50%
- 结论：约 10%
- 参考文献：约 15%

## 6. 推荐参考文献方向

由于篇幅只有两页，建议保留 4-6 篇核心参考文献。

推荐类别：

1. 反谐振空芯光纤基础文献。
2. UPPE 或宽带超快传播数值模拟文献。
3. 气体填充空芯光纤中的紫外产生 / 色散波产生文献。
4. RNN / LSTM 预测非线性光传播文献。
5. Transformer 或神经网络代理模型在光子学中的应用。
6. 物理信息神经网络或数据驱动非线性光学建模文献。

## 7. 表述注意事项

根据最终训练数据和模型结果选择准确表述。

如果使用 `per_sample_minmax`：

> 模型预测的是归一化 log 光谱形状。

不要声称预测了绝对功率或真实强度。

如果使用 `global_log_standard`：

> 模型预测的是全局标准化 log-power 光谱，保留了样本间相对强度差异。

如果 Transformer 不是最优模型：

> Transformer 模型验证了序列感知代理建模的可行性，而卷积型时空 decoder 在预测平滑性和计算效率方面提供了有价值的对照。

如果最终 Transformer 结果不支持 `R² > 0.9`：

应将：

> Test results show good agreement, with R² exceeding 0.9.

改为：

> Test results demonstrate the feasibility of data-driven prediction of spectral evolution, with performance depending on model architecture and spectral region.

## 8. 中文摘要草稿

> 反谐振空芯光纤为气体非线性频率转换和相干紫外光产生提供了灵活平台。该类光纤中的超快脉冲传播涉及波导色散、Kerr 非线性、等离子体响应和气体电离等多物理过程，通常需要高保真的 UPPE 数值仿真进行描述，但其计算成本较高，不利于快速参数扫描和结构优化。本文提出一种基于 Transformer 的代理建模方法，利用 UPPE 仿真数据学习气体填充反谐振空芯光纤中的宽带光谱传播动力学。模型以光纤结构、气体压强和输入脉冲参数为输入，预测 `200-2500 nm` 范围内的光谱演化及紫外色散波相关特征。通过引入传播距离嵌入、紫外波段加权损失和早期传播加权约束，模型对短波段和传播初期动力学具有更强的学习能力。该方法为反谐振空芯光纤紫外产生的快速预测、参数优化和结构设计提供了一种 AI 辅助建模思路。

## 9. 正式写作前检查清单

1. 确认最终 Transformer v2 或后续模型的真实指标。
2. 决定论文报告的是：
   - 归一化光谱形状预测，还是
   - log-power 真实强度预测。
3. 生成最终图：
   - 工作流程图
   - 最终光谱对比图
   - 光谱传播演化对比图
   - 可选模型对比表
4. 从 `metrics.json` 中核对所有 R² 和误差指标。
5. 保证摘要、正文和图注中的性能表述与实际训练结果一致。
